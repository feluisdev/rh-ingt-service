-- =============================================================
-- V1 — Criar schema de auditoria para Hibernate Envers
-- =============================================================

CREATE SCHEMA IF NOT EXISTS audit_schema;
