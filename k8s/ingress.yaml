apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sipprog-rh-backend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: wayvant-test.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sipprog-rh-backend-service
                port:
                  number: 8080
